# Frontend production environment variables
#
# The backend URL is NOT read from an env var at runtime — /api/* is proxied
# to the backend via the rewrite rule in vercel.json. Update that file's
# destination if the backend URL changes.
